export const EARTH_RADIUS = 0.6371;
export const SUN_RADIUS = EARTH_RADIUS * 109;
export const MERCURY_RADIUS = EARTH_RADIUS * 0.38;
export const VENUS_RADIUS = EARTH_RADIUS * 0.95;
export const MARS_RADIUS = EARTH_RADIUS * 0.53;
export const EARTH_DISTANCE = 147.47;
export const MERCURY_DISTANCE = EARTH_DISTANCE * 0.387;
export const VENUS_DISTANCE = EARTH_DISTANCE * 0.723;
export const MARS_DISTANCE = EARTH_DISTANCE * 1.524;
const EARTH_FLATTENING_FACTOR = 1 / 29825;
export const EARTH_POLAR_RADIUS = EARTH_RADIUS * (1 - EARTH_FLATTENING_FACTOR);
export const MERCURY_POLAR_RADIUS = MERCURY_RADIUS * (1 - 0.00006);
export const VENUS_POLAR_RADIUS = VENUS_RADIUS * (1 - 0.000015);
export const MARS_POLAR_RADIUS = MARS_RADIUS * (1 - 0.00648);
export const SUN_POLAR_RADIUS = SUN_RADIUS * (1 - 0.00015);
export const EARTH_YEAR = 10 * Math.PI * (1 / 60) * (1 / 60); // 107.000 km/h
export const EARTH_DAY = EARTH_YEAR / 1674.4; // 1674.4 km/h
const EARTH_AXIAL_TILT = (23.5 * Math.PI) / 180;
